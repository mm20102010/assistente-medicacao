# Diário de Medicação 4.0

Baseline MM Registro 4.0 com preservação integral das regras funcionais do Diário. O Core compartilhado é 4.0.0, os nomes nativos passam a localizar `CFBundleDisplayName` e `CFBundleName`, e as proteções nativas já validadas (incluindo `contentInset: never`) permanecem ativas.
