# Diário 4.0.6

Equalização de hardening genérico: fast path iPhone→Watch para estado substituível mantendo `applicationContext` como autoridade durável, retenção de 16 epochs, proteção contra regressão visual `ACK × didFinish` e sync nativo determinístico sem dependência do Capacitor CLI para a cópia rotineira do bundle. Sem alteração do registro, IndexedDB, ACK fim-a-fim, reset/cutoff ou MM Registro Core.
